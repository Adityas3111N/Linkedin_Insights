# Database connection string (MySQL or PostgreSQL)
DATABASE_URL=mysql+pymysql://root:password@localhost:3306/linkedin_insights

# Application Settings
APP_ENV=development
APP_DEBUG=True
API_V1_STR=/api/v1

# LinkedIn credentials for authenticated scraping (optional, falls back to seed/mock data if empty)
LINKEDIN_EMAIL=
LINKEDIN_PASSWORD=
